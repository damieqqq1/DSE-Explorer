# dse-explorer 使用说明

`dse-explorer` 是一个面向设计空间探索（DSE）、多目标优化（MOO）和组合优化研究的论文研究 Agent 项目。它结合了本地 PDF 论文库、Qdrant 向量数据库、Hybrid Search、Reranker、LangGraph、MCP Server、DeepSeek LLM、会话记忆和结构化论文分析能力。

## 1. 环境准备

进入项目目录：

```powershell
cd C:\Users\Administrator\Desktop\Code\dse-explorer
```

使用 Conda 环境运行：

```powershell
conda activate dse_explorer
```

如果 PowerShell 没有初始化 Conda，可以使用：

```powershell
conda run --no-capture-output -n dse_explorer python main.py --check
```

## 2. 配置检查

项目配置在 `.env` 中，包括：

- DeepSeek LLM API。
- DashScope Embedding API。
- Qdrant 向量数据库。
- Tavily / SerpApi 互联网检索 API。

检查配置：

```powershell
python main.py --check
```

查看脱敏配置：

```powershell
python main.py --show-config
```

## 3. PDF 论文入库

本地论文目录：

```text
Paper_ljh/
```

小规模入库测试：

```powershell
python -m rag_pipeline.ingest --limit 3
```

全量入库：

```powershell
python -m rag_pipeline.ingest --all --batch-size 10
```

只解析和切块，不写入 Qdrant：

```powershell
python -m rag_pipeline.ingest --all --dry-run
```

入库流程：

```text
扫描 PDF -> pypdf 逐页解析 -> 文本清洗 -> chunk 切分 -> embedding -> Qdrant upsert
```

当前 Qdrant collection：

```text
dse_papers
```

## 4. Hybrid Retrieval 检索

当前检索器默认使用 Hybrid Search + Reranker：

```text
Query Rewrite
-> Qdrant Dense Retrieval
-> BM25 Sparse Keyword Retrieval
-> RRF 融合
-> Lightweight Reranker
```

直接检索本地论文库：

```powershell
python -m rag_pipeline.retriever "GRL-DSE graph reinforcement learning design space exploration" --top-k 3 --candidate-k 30
```

禁用 BM25，只使用向量检索：

```powershell
python -m rag_pipeline.retriever "LLM design space exploration" --top-k 5 --dense-only
```

禁用 rerank：

```powershell
python -m rag_pipeline.retriever "LLM design space exploration" --top-k 5 --no-rerank
```

返回内容包括：

- 综合相关性分数。
- dense score。
- sparse score。
- fused score。
- rerank score。
- PDF 路径。
- 页码。
- 文本片段。

## 5. 论文元数据和方法信息抽取

项目可以从 PDF 前几页自动抽取结构化论文信息，保存到本地 SQLite：

```text
rag_pipeline/data/paper_info.sqlite3
```

抽取前 3 篇：

```powershell
python -m rag_pipeline.paper_info --limit 3
```

全量抽取：

```powershell
python -m rag_pipeline.paper_info --all
```

只打印结果，不写入 SQLite：

```powershell
python -m rag_pipeline.paper_info --limit 1 --dry-run
```

查看最近抽取记录：

```powershell
python -m rag_pipeline.paper_info --list --limit 10
```

也可以通过 `main.py` 运行：

```powershell
python main.py --extract-paper-info --paper-info-limit 3
python main.py --paper-info-list
```

元数据字段包括：

```text
title, authors, year, venue, abstract, keywords
```

方法信息字段包括：

```text
method_name, problem, core_idea, optimization_algorithm, model_or_agent,
design_variables, objectives, benchmarks, baselines, metrics,
main_results, limitations
```

## 6. DSE 方法对比矩阵

项目可以基于 `paper_info.sqlite3` 生成 DSE/MOO 方法对比矩阵。

生成 Markdown 表格：

```powershell
python -m rag_pipeline.method_matrix --limit 20 --format markdown
```

按关键词筛选：

```powershell
python -m rag_pipeline.method_matrix --keyword "Bayesian optimization" --format markdown
```

生成 CSV：

```powershell
python -m rag_pipeline.method_matrix --keyword "GRL-DSE" --format csv
```

保存到 `reports/`：

```powershell
python -m rag_pipeline.method_matrix --limit 20 --save-path reports/dse_method_matrix.md
```

矩阵默认字段包括：

```text
Method, Paper, Year, Problem, Core Idea, Optimizer, Model/Agent,
Objectives, Benchmarks, Baselines, Metrics, Main Results, Limitations, Source
```

该能力也作为 MCP tool 暴露：

```text
generate_dse_method_matrix
```

## 7. 启动 MCP Server

stdio 模式：

```powershell
python -m mcp_server.server --transport stdio
```

HTTP 模式：

```powershell
python -m mcp_server.server --transport http --host 127.0.0.1 --port 8765
```

当前 MCP tools：

```text
search_dse_papers
get_rag_corpus_stats
generate_dse_method_matrix
search_research_web
```

### search_dse_papers

检索本地 DSE/MOO 论文知识库。当前默认使用 Hybrid Search + Reranker。

适合问题：

- GRL-DSE 的核心思想是什么？
- Bayesian Optimization 如何用于 DSE？
- LLM-DSE 有哪些代表方法？

### get_rag_corpus_stats

查看当前 Qdrant 论文库状态，包括 collection、向量维度、points 数量和状态。

### generate_dse_method_matrix

从 `paper_info.sqlite3` 生成结构化 DSE 方法对比矩阵。

适合问题：

- 比较 GRL-DSE 和 BOOM-Explorer。
- 总结使用 Bayesian Optimization 的 DSE 方法。
- 列出不同方法的优化目标、baseline 和 metrics。

### search_research_web

检索互联网上关于设计空间探索或组合优化的研究资料。默认优先使用 Tavily，失败时可切换到 SerpApi。

## 8. 使用 Agent 问答

Agent 入口：

```powershell
python main.py --ask "GRL-DSE 的核心思想是什么？"
```

当前 Agent 流程：

```text
Planner -> Query Rewriter -> MCP Hybrid RAG Executor -> Reasoner -> Synthesizer
```

其中：

- Planner 生成检索计划。
- Query Rewriter 改写检索 query，并生成 query variants。
- Executor 通过 MCP 调用 `search_dse_papers`。
- Reasoner 判断证据是否足够。
- Synthesizer 生成最终回答和引用来源。

查看 trace：

```powershell
python main.py --ask "GRL-DSE 的核心思想是什么？" --show-trace
```

控制最大检索/推理轮数：

```powershell
python main.py --ask "LLM-DSE 和传统 DSE 有什么区别？" --max-iterations 3
```

## 9. 多轮上下文和记忆

项目使用本地 SQLite 保存会话和长期记忆：

```text
memory/dse_explorer_memory.sqlite3
```

每轮问答结束后保存：

- 用户问题。
- Agent 回答。
- 引用来源。
- 问答摘要。
- 长期记忆条目。

指定 session：

```powershell
python main.py --ask "GRL-DSE 的核心思想是什么？" --session demo
python main.py --ask "它用了什么优化方法？" --session demo --show-memory
```

控制注入最近几轮：

```powershell
python main.py --ask "这个方法和 BOOM-Explorer 有什么区别？" --session demo --memory-turns 3
```

禁用记忆：

```powershell
python main.py --ask "它用了什么优化方法？" --no-memory
```

## 10. 直接调用 DeepSeek

如果只想测试 LLM，不走 RAG 和 Agent：

```powershell
python main.py --prompt "请用一句话解释设计空间探索。"
```

## 11. 推荐使用流程

第一次使用建议按这个顺序：

```text
1. main.py --check
2. rag_pipeline.ingest --limit 3
3. rag_pipeline.retriever 查询 smoke test
4. rag_pipeline.paper_info --limit 3
5. rag_pipeline.method_matrix --limit 10
6. main.py --ask ... --show-trace
```

## 12. 当前能力边界

已经支持：

- 本地 PDF 论文 RAG。
- Qdrant 向量入库。
- Hybrid Search。
- BM25 Sparse Keyword Search。
- Lightweight Reranker。
- Query Rewriter。
- MCP Server 和 MCP Client。
- LangGraph Agent。
- Reasoner 迭代检索。
- 会话记忆和长期记忆。
- 论文元数据和方法信息抽取。
- DSE 方法对比矩阵 MCP tool。
- 互联网研究搜索 MCP tool。

暂未完整支持：

- Web UI。
- RAG 自动评估集。
- PDF 图表、公式、表格的结构化解析。
- 自动把互联网搜索结果写入长期知识库。
- 更复杂的 tool router 和多 Agent 协作。

## 13. 常见问题

### PowerShell 中 conda activate 失败

如果提示 `Run 'conda init' before 'conda activate'`，可以优先使用：

```powershell
conda run --no-capture-output -n dse_explorer python main.py --check
```

### Windows 输出乱码

优先使用：

```powershell
conda run --no-capture-output -n dse_explorer python ...
```

### PDF 解析失败

少量 PDF 可能损坏或不是文本型 PDF。当前入库流程会跳过失败文件，并继续处理其他论文。扫描版 PDF 需要额外 OCR 支持。

### 方法矩阵为空

先运行论文信息抽取：

```powershell
python -m rag_pipeline.paper_info --limit 10
```

再运行：

```powershell
python -m rag_pipeline.method_matrix --limit 10
```
