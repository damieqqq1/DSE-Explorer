# dse-explorer 项目面试准备

## 1. 项目一句话介绍

`dse-explorer` 是一个面向设计空间探索（Design Space Exploration, DSE）、多目标优化（Multi-Objective Optimization, MOO）和组合优化研究的论文问答 Agent 项目。

它把本地 DSE 论文库解析成向量知识库，通过 Qdrant 做语义检索，再结合 DeepSeek LLM、LangGraph Agent 流程和 MCP 工具调用，生成有论文证据和来源页码支撑的回答。

## 2. 项目解决的问题

这个项目主要解决研究场景中的几个痛点：

- DSE/MOO 相关论文数量多，人工逐篇查找和比较效率低。
- 研究问题通常不是简单关键词搜索，而需要跨论文归纳方法、比较差异、总结优化策略。
- LLM 直接回答容易缺少依据，因此项目使用本地论文 RAG 来增强回答可靠性。
- 多轮研究问答中会出现“它”“这个方法”等指代问题，因此项目加入了会话记忆和长期记忆。
- 工具调用逻辑需要标准化，因此项目通过 MCP Server 暴露检索和网页搜索能力。

## 3. 核心功能

### 3.1 本地论文入库

项目可以读取 `Paper_ljh/` 下的 PDF 论文，完成：

- PDF 文本解析。
- 文本清洗和切分 chunk。
- 调用 embedding 模型生成向量。
- 写入 Qdrant 向量数据库。
- 使用本地 parsed cache 避免重复解析 PDF。

对应模块：

- `rag_pipeline/ingest.py`

### 3.2 本地论文语义检索

项目可以基于自然语言问题检索本地论文库，返回：

- 相似度分数。
- PDF 文件路径。
- 页码。
- 相关文本片段。

对应模块：

- `rag_pipeline/retriever.py`

### 3.3 MCP 工具服务

项目实现了一个 MCP Server，把检索能力封装成标准工具：

- `search_dse_papers`：检索本地 DSE/MOO 论文知识库。
- `get_rag_corpus_stats`：查看 Qdrant collection 状态。
- `search_research_web`：搜索互联网上的 DSE 或组合优化研究资料。

对应模块：

- `mcp_server/server.py`
- `mcp_server/tools/qdrant_rag.py`
- `mcp_server/tools/web_search.py`

### 3.4 Paper-Grounded Agent 问答

用户通过 `main.py --ask` 提问后，系统会启动一个基于论文证据的 Agent 流程：

```text
Planner -> Executor -> Reasoner -> Synthesizer
```

各节点职责：

- `Planner`：把用户问题拆解成一个或多个检索任务。
- `Executor`：通过 MCP client 调用论文检索工具。
- `Reasoner`：评估当前证据是否足够，必要时追加 follow-up 检索。
- `Synthesizer`：基于证据生成最终回答，并输出来源。

对应模块：

- `agent/graph.py`
- `agent/nodes/planner.py`
- `agent/nodes/executor.py`
- `agent/nodes/reasoner.py`

### 3.5 多轮会话和长期记忆

项目使用本地 SQLite 保存：

- 用户问题。
- Agent 回答。
- 引用来源。
- 问答摘要。
- 长期记忆条目。

后续提问时，系统会把相关历史上下文注入 Planner、Reasoner 和 Synthesizer，提升多轮问答中的连续性。

对应模块：

- `utils/memory.py`

### 3.6 配置检查和直接 LLM 调用

项目入口 `main.py` 还支持：

- `--check`：检查本地论文目录、模型配置、Qdrant 配置。
- `--show-config`：打印脱敏后的配置摘要。
- `--prompt`：直接调用 DeepSeek 兼容 LLM，不走 RAG 和 Agent。

对应模块：

- `main.py`
- `utils/config.py`
- `utils/deepseek_llm.py`

## 4. 使用的 Agent 范式

这个项目不是纯 ReAct Agent，而是更接近：

```text
Plan-and-Execute + RAG + Reflection / Iterative Retrieval + Memory-Augmented Agent
```

具体表现：

- Plan-and-Execute：先由 Planner 生成检索计划，再由 Executor 执行。
- RAG Agent：回答依赖本地论文向量库检索出的 evidence。
- Reflection / Self-Refinement：Reasoner 会判断证据是否充分，不充分则追加检索。
- Tool-using Agent：通过 MCP client 调用 MCP Server 暴露的工具。
- Memory-Augmented Agent：使用会话记忆和长期记忆处理多轮上下文。

## 5. 主要技术栈

### 5.1 Python

项目主体使用 Python 实现，入口是 `main.py`。

### 5.2 LangGraph

LangGraph 用来定义 Agent 的状态流转图，负责组织 Planner、Executor、Reasoner、Synthesizer 节点。

### 5.3 LangChain / langchain-openai

用于和 LLM、embedding 等能力衔接，项目中封装了 DeepSeek 兼容模型调用。

### 5.4 DeepSeek LLM

项目使用 DeepSeek Chat 作为主要语言模型，负责：

- 生成检索计划。
- 判断证据是否充分。
- 综合最终回答。
- 总结会话和抽取长期记忆。

### 5.5 Qdrant

Qdrant 是向量数据库，用于保存论文 chunk 的 embedding，并支持语义相似度检索。

### 5.6 Embedding 模型

项目配置中使用 DashScope `text-embedding-v3` 生成论文 chunk 和用户 query 的向量表示。

### 5.7 MCP / FastMCP

MCP 用来把论文检索和网页搜索能力封装成标准工具。Agent 的 Executor 通过 MCP client 调用这些工具。

### 5.8 SQLite

SQLite 用于本地保存会话历史和长期记忆，文件位于：

```text
memory/dse_explorer_memory.sqlite3
```

### 5.9 Tavily / SerpApi

用于互联网研究检索，作为本地论文库之外的补充信息来源。

## 6. 面试讲解重点

面试时可以按下面顺序介绍：

1. 先讲项目背景：DSE/MOO 论文研究需要高效检索、总结和比较。
2. 再讲整体架构：PDF 入库、Qdrant RAG、MCP 工具、LangGraph Agent。
3. 然后讲 Agent 流程：Planner、Executor、Reasoner、Synthesizer。
4. 重点说明为什么不是简单 RAG：它有规划、工具调用、证据评估、迭代检索和记忆。
5. 最后讲工程化点：配置管理、缓存、引用来源、SQLite 记忆、MCP 工具化。

## 7. 后续待补充解析

后续可以继续补充：

- 项目启动流程解析。
- `main.py` 执行链路解析。
- LangGraph 状态设计解析。
- Planner 节点解析。
- Executor 和 MCP 调用链路解析。
- RAG 入库和检索流程解析。
- Reasoner 迭代检索机制解析。
- Synthesizer 如何生成带来源回答。
- Memory 机制解析。
- 面试常见问题和回答话术。

## 8. PDF 如何处理并进入向量库

PDF 入库流程主要由 `rag_pipeline/ingest.py` 完成，整体链路可以概括为：

```text
扫描 PDF -> 逐页解析文本 -> 文本清洗 -> 切分 chunk -> 生成稳定 chunk id
-> 调用 embedding 模型 -> 初始化 Qdrant collection -> upsert 向量和 metadata
```

### 8.1 入口命令

小规模测试入库：

```powershell
python -m rag_pipeline.ingest --limit 3
```

全量入库：

```powershell
python -m rag_pipeline.ingest --all --batch-size 10
```

只解析和切分，不写入向量库：

```powershell
python -m rag_pipeline.ingest --all --dry-run
```

### 8.2 扫描 PDF 文件

`find_pdfs()` 会在配置的 `PAPER_ROOT` 目录下递归查找所有 `.pdf` 文件。

项目默认论文目录是：

```text
Paper_ljh/
```

如果命令传了 `--limit 3`，就只取前 3 篇；如果传 `--all`，就处理全部 PDF。

面试表达：

> 项目首先从本地论文目录递归扫描 PDF。这个目录由 `.env` 中的 `PAPER_ROOT` 配置，默认是项目下的 `Paper_ljh/`。

### 8.3 解析 PDF 文本

`extract_pdf_chunks()` 使用 `pypdf.PdfReader` 打开 PDF，然后逐页读取：

```python
reader = PdfReader(str(pdf_path))
for page_index, page in enumerate(reader.pages, start=1):
    text = normalize_text(page.extract_text() or "")
```

这里会保留页码，因为后续回答需要输出“证据来自哪篇 PDF 的第几页”。

如果某一页没有提取出文本，就跳过该页。

面试表达：

> 每篇 PDF 会被逐页解析，系统不仅保存文本，还保存页码、文件名、相对路径等元信息。这样最终回答可以带上论文来源和页码，而不是只给一个没有出处的总结。

### 8.4 文本清洗

`normalize_text()` 会做基础清洗：

- 去掉异常空字符。
- 用 UTF-8 忽略非法字符。
- 把连续空白压缩成单个空格。

这样做的目的是让后续 chunk 更稳定，减少 PDF 抽取造成的格式噪声。

### 8.5 文本切分 chunk

`split_text()` 按固定字符长度切分文本：

- 默认 `chunk_size = 1200`
- 默认 `chunk_overlap = 180`

overlap 的作用是减少切块边界造成的语义断裂。例如一个方法描述跨越两个 chunk，重叠区可以让检索时仍然保留上下文。

面试表达：

> 项目采用固定长度加 overlap 的方式切分论文文本。这样既能控制单个向量的文本长度，又能避免关键语义刚好被切断。

### 8.6 生成 chunk id 和 metadata

每个 chunk 会生成一个稳定 ID：

```python
uuid.uuid5(uuid.NAMESPACE_URL, raw)
```

raw 中包含：

- PDF 相对路径。
- 页码。
- chunk index。
- chunk 前 128 个字符。

这意味着同一篇论文、同一页、同一段文本在重复入库时会得到相同 ID，方便幂等 upsert。

每个 chunk 同时保存 metadata：

```text
source: PDF 绝对路径
relative_path: 相对论文目录的路径
file_name: PDF 文件名
category: 所在子目录
page: 页码
chunk_index: 当前页内 chunk 序号
project: dse-explorer
```

面试表达：

> 向量库里不只存 embedding，还会存 metadata。metadata 是后面做引用来源、按项目过滤、按论文路径定位证据的关键。

### 8.7 parsed cache 缓存

项目会把 PDF 解析和切分后的结果缓存到：

```text
rag_pipeline/data/parsed_cache/
```

缓存文件名由 PDF 相对路径、chunk size、chunk overlap 做 SHA1 得到。

缓存校验会比较：

- PDF 文件大小。
- PDF 修改时间。
- chunk size。
- chunk overlap。

如果这些信息没有变化，就直接复用缓存，不重新解析 PDF。

面试表达：

> PDF 解析是比较慢的步骤，所以项目做了 parsed cache。只要 PDF 文件和切分参数没变，重复入库时可以直接读取缓存，提升开发和调试效率。

### 8.8 生成 embedding

`upsert_chunks()` 会把 chunk 文本批量交给 `EmbeddingClient.embed_documents()`。

EmbeddingClient 支持几类 embedding：

- `dashscope`
- `openai`
- `openai_compatible`
- `hash`

当前项目配置使用 DashScope：

```text
text-embedding-v3
```

并且向量维度由 Qdrant 配置控制，当前配置中是：

```text
vector_size = 512
```

面试表达：

> 每个 chunk 会通过 embedding 模型转成一个定长向量。当前项目使用 DashScope 的 text-embedding-v3，并通过 OpenAI-compatible 的 `/embeddings` 接口形态调用。

### 8.9 初始化 Qdrant collection

在真正写入前，`ensure_collection()` 会检查 Qdrant collection 是否存在。

如果不存在，就创建 collection：

```text
collection: dse_papers
distance: cosine
vector_size: 512
```

同时会给几个 payload 字段建索引：

```text
project
relative_path
category
```

这些索引用于后续过滤和检索优化。

面试表达：

> 项目写入 Qdrant 前会自动确保 collection 存在，并创建 payload index。这样检索时可以按项目、路径、类别等 metadata 做过滤。

### 8.10 写入 Qdrant

`upsert_chunks()` 最后会构造 `PointStruct`：

```python
PointStruct(
    id=chunk.id,
    vector=vector,
    payload={**chunk.metadata, "text": chunk.text},
)
```

写入内容包括：

- `id`：稳定 chunk id。
- `vector`：embedding 向量。
- `payload.text`：原始 chunk 文本。
- `payload.metadata`：论文路径、页码、类别等信息。

然后调用：

```python
client.upsert(collection_name=settings.qdrant.collection, points=points)
```

`upsert` 的含义是：如果 id 已存在则更新，不存在则插入，因此重复入库不会简单产生重复数据。

### 8.11 失败处理

在 `ingest_pdfs()` 中，每篇 PDF 的解析被 try/except 包裹。

如果某篇 PDF 解析失败：

- 记录日志。
- 跳过当前 PDF。
- 继续处理后面的 PDF。

面试表达：

> 入库流程对单篇 PDF 失败做了隔离处理，一篇论文解析失败不会中断整个批量入库任务。

### 8.12 面试版总结

可以这样回答：

> 这个项目的 PDF 入库链路在 `rag_pipeline/ingest.py`。它先从 `Paper_ljh/` 递归扫描 PDF，然后用 `pypdf` 逐页抽取文本，做基础清洗，再按固定长度和 overlap 切成 chunk。每个 chunk 会保存文本、页码、文件名、相对路径、类别等 metadata，并用路径、页码、chunk index 和文本内容生成稳定 ID。之后项目通过 `EmbeddingClient` 调用 DashScope `text-embedding-v3` 生成向量，最后把向量和 payload 一起 upsert 到 Qdrant 的 `dse_papers` collection。为了提升效率，它还做了 parsed cache，PDF 和切分参数没变时可以复用解析结果。

## 9. pypdf 是怎么解析 PDF 文本的

项目中使用的 `pypdf` 版本是 `6.10.2`。代码调用点在 `rag_pipeline/ingest.py`：

```python
reader = PdfReader(str(pdf_path))
for page_index, page in enumerate(reader.pages, start=1):
    text = normalize_text(page.extract_text() or "")
```

也就是说，项目并没有自己实现 PDF 底层解析，而是把逐页文本抽取交给了 `pypdf.PageObject.extract_text()`。

### 9.1 pypdf 不是 OCR

首先要明确一点：`pypdf` 的文本提取不是 OCR。

它只能提取 PDF 内容流里真实存在的文字绘制指令。如果 PDF 是扫描版图片，页面上只有图片，没有可复制的文字，那么 `pypdf` 通常提取不到有效文本。

面试表达：

> pypdf 适合处理文本型 PDF，它是解析 PDF 内部的文本绘制指令，不是做图像识别。如果论文 PDF 是扫描件，就需要额外接 OCR。

### 9.2 PDF 页面里文字的本质

PDF 不是像 HTML 那样天然保存段落结构。PDF 页面更像一组绘制指令：

```text
选择字体
移动到某个坐标
绘制一段文字
再移动到另一个坐标
继续绘制文字
```

所以 `pypdf` 的文本提取，本质上是从页面的 content stream 里找出“显示文字”的操作符，再尽量把它们拼成普通字符串。

常见文本相关操作符包括：

- `BT`：Begin Text，开始一个文本对象。
- `ET`：End Text，结束一个文本对象。
- `Tf`：设置字体和字号。
- `Tm`：设置文本矩阵。
- `Td`：移动文本位置。
- `T*`：移动到下一行。
- `Tj`：显示一段文本。
- `TJ`：显示文本数组，数组中可能包含字距调整。
- `'` 和 `"`：换行后显示文本的简写操作。

### 9.3 pypdf 的大致处理流程

`page.extract_text()` 默认使用 `plain` 模式。它的大致流程是：

```text
读取页面 /Resources
-> 找到字体资源 /Font
-> 解析字体编码和 ToUnicode CMap
-> 读取页面 /Contents 内容流
-> 遍历内容流中的 PDF 操作符
-> 遇到文本显示操作符时解码文字
-> 根据文本矩阵和字体信息推断空格、换行
-> 拼接成最终字符串返回
```

对应到 pypdf 内部，大致是：

- `PageObject.extract_text()`：外部入口。
- `PageObject._extract_text()`：读取资源、内容流并遍历操作符。
- `TextExtraction`：维护文本提取状态，例如当前字体、字号、文本矩阵、坐标变换矩阵、累计输出。
- `_cmap.py`：处理字体编码和 `/ToUnicode` CMap。

### 9.4 字体和编码如何影响文本

PDF 里的 `Tj` / `TJ` 操作符里存的未必直接是 Unicode 字符。

它可能是：

- 字节串。
- 字体内部字符编码。
- glyph id。
- 自定义编码。

所以 pypdf 需要根据页面资源中的字体信息来解码：

- 如果字体有 `/ToUnicode`，优先用它把字符码映射成 Unicode。
- 如果没有 `/ToUnicode`，尝试根据 `/Encoding`、`/BaseEncoding`、预定义 CMap 或字体信息推断。
- 如果映射缺失，提取结果可能乱码或缺字。

面试表达：

> pypdf 提取文本的关键不只是找到 Tj/TJ 操作，还要把 PDF 内部字符编码映射成 Unicode。这个映射主要依赖字体资源和 ToUnicode CMap，所以有些 PDF 如果字体映射不完整，抽出来的文本可能乱码。

### 9.5 TJ 为什么特殊

`Tj` 通常表示显示一段文本。

`TJ` 表示显示一个数组，数组里可能混合：

```text
字符串片段 + 数字间距调整 + 字符串片段
```

例如一个 PDF 为了控制排版，会把一个单词拆成多个片段，并在中间放字距调整。pypdf 在处理 `TJ` 时，会遍历数组：

- 遇到字符串就当成 `Tj` 处理。
- 遇到较大的间距数值时，可能推断这里应该插入空格。

这就是为什么 PDF 文本提取经常需要“猜”空格和换行。

### 9.6 换行和空格是推断出来的

PDF 里很多时候并没有显式的“这是一个段落”或“这里有一个空格”的结构。

pypdf 会根据：

- 当前文本矩阵。
- 坐标变化。
- 字体大小。
- 字符宽度。
- space width。
- `Td`、`Tm`、`T*` 等位置变化操作。

来判断是否需要插入空格或换行。

这也是为什么不同 PDF 生成器导出的论文，文本抽取质量会有差异。

面试表达：

> PDF 是面向版面绘制的格式，不是面向语义结构的格式。pypdf 只能根据绘制顺序、坐标、字体宽度去推断阅读顺序、空格和换行，所以它对不同 PDF 生成器的效果不完全一致。

### 9.7 plain 模式和 layout 模式

pypdf 的 `extract_text()` 支持两类模式：

```python
page.extract_text(extraction_mode="plain")
page.extract_text(extraction_mode="layout")
```

项目当前使用的是默认 `plain` 模式，也就是：

```python
page.extract_text()
```

`plain` 模式更偏向按内容流顺序提取文本，适合 RAG 入库前的纯文本抽取。

`layout` 模式会尝试更好地保留页面布局，例如基于坐标和固定宽度字符宽度恢复排版，但它是更复杂的布局还原逻辑，对 RAG 来说不一定总是更合适。

### 9.8 这个项目为什么这样用

项目只需要把论文内容转成可检索文本，不需要完全还原 PDF 视觉版式。因此当前实现选择了最直接稳定的方式：

```python
page.extract_text()
```

后续再通过：

```python
normalize_text()
split_text()
embedding
```

把提取出的文本变成向量库中的知识片段。

这种设计的优点：

- 实现简单。
- 依赖轻。
- 适合文本型英文论文。
- 可以保留页码作为 citation。

局限：

- 扫描版 PDF 无法处理。
- 图表、公式、表格结构不能很好保留。
- 多栏论文的阅读顺序可能不完美。
- 字体编码异常时可能出现乱码。

### 9.9 面试版总结

可以这样回答：

> 这个项目用 pypdf 做 PDF 文本抽取。pypdf 不是 OCR，它是读取 PDF 页面的 content stream，解析其中的文本绘制操作，比如 BT/ET、Tj、TJ、Tf、Tm、Td 等。它会结合页面资源里的 Font、Encoding 和 ToUnicode CMap，把 PDF 内部字符编码转换成 Unicode 文本。同时它会根据文本矩阵、坐标变化、字体大小和字符宽度推断空格和换行。项目里每一页调用 `page.extract_text()`，拿到文本后再做清洗、切块、embedding，最后写入 Qdrant。这个方案适合文本型论文 PDF，但对扫描件、复杂表格、公式和多栏排版会有一定局限。

## 10. LangGraph 在项目中是什么，怎么发挥作用

### 10.1 LangGraph 是什么

LangGraph 是一个用“图”来编排 Agent 工作流的框架。

它把 Agent 的执行过程拆成多个节点，每个节点负责一件明确的事；节点之间通过边连接；所有节点共享一个状态对象。这样 Agent 不再是一个不可控的长 prompt，而是一个结构化、可追踪、可循环的状态机。

在这个项目里，LangGraph 不是负责向量检索，也不是负责 LLM 生成本身。它的作用是编排整个论文问答 Agent 的流程。

面试表达：

> LangGraph 在这个项目里相当于 Agent 的流程编排层。它把规划、工具检索、证据评估和最终回答拆成多个节点，并通过共享状态和条件边控制执行顺序。

### 10.2 项目中的 LangGraph 流程

项目的图定义在 `agent/graph.py`。

核心流程是：

```text
Planner -> Executor -> Reasoner -> Synthesizer -> END
```

但这里不是简单的单向链路。`Reasoner` 后面有一个条件分支：

```text
Reasoner
  -> 如果证据不足：回到 Executor 继续检索
  -> 如果证据足够：进入 Synthesizer 生成最终答案
```

因此完整逻辑可以表示为：

```text
Planner
  -> Executor
  -> Reasoner
      -> continue -> Executor
      -> finish   -> Synthesizer
  -> END
```

这就是项目实现“迭代检索”的关键。

### 10.3 StateGraph 如何定义

项目使用：

```python
graph = StateGraph(AgentState)
```

这里的 `AgentState` 是整个 Agent 流程共享的数据结构，定义在 `agent/state.py`。

它包含：

```text
question: 用户问题
conversation_context: 最近会话上下文
long_term_context: 长期记忆上下文
plan: Planner 生成的检索计划
executed_step_ids: 已执行过的计划步骤
evidence: Executor 检索到的证据
iteration: 当前推理轮次
max_iterations: 最大迭代次数
needs_more_evidence: 是否需要更多证据
reasoner_note: Reasoner 的判断说明
compressed_context: 压缩后的证据上下文
final_answer: 最终回答
sources: 引用来源
```

面试表达：

> LangGraph 的状态对象是这个 Agent 的“工作台”。每个节点不是孤立执行，而是读取和更新同一个 AgentState，比如 Planner 写入 plan，Executor 写入 evidence，Reasoner 写入 needs_more_evidence，Synthesizer 写入 final_answer。

### 10.4 各节点怎么接入 LangGraph

在 `agent/graph.py` 中，项目把各个 Python 函数注册为图节点：

```python
graph.add_node("planner", planner_node)
graph.add_node("executor", executor_node)
graph.add_node("reasoner", reasoner_node)
graph.add_node("synthesizer", synthesizer_node)
```

每个节点本质上都是一个函数：

- 输入：当前 `AgentState`。
- 输出：需要更新到状态里的字段。

例如：

- `planner_node` 返回 `{"plan": plan}`。
- `executor_node` 返回 `{"evidence": evidence, "executed_step_ids": executed_step_ids}`。
- `reasoner_node` 返回 `{"needs_more_evidence": True/False, ...}`。
- `synthesizer_node` 返回 `{"final_answer": answer, "sources": sources}`。

LangGraph 会把节点返回的字段合并回共享状态。

### 10.5 条件边怎么实现迭代检索

项目中最关键的是这段：

```python
graph.add_conditional_edges(
    "reasoner",
    should_continue_retrieval,
    {
        "continue": "executor",
        "finish": "synthesizer",
    },
)
```

条件函数是：

```python
def should_continue_retrieval(state: AgentState) -> str:
    return "continue" if state.get("needs_more_evidence") else "finish"
```

也就是说，`Reasoner` 会判断当前证据是否足够：

- 如果 `needs_more_evidence = True`，LangGraph 把流程送回 `Executor`。
- 如果 `needs_more_evidence = False`，LangGraph 进入 `Synthesizer`。

这让项目具备了一个简单但有效的 self-refinement 能力：不是检索一次就回答，而是可以先判断证据质量，再决定是否继续检索。

面试表达：

> 条件边是这个项目里 LangGraph 发挥价值最大的地方。Reasoner 不只是生成说明，它会把 `needs_more_evidence` 写入状态，LangGraph 根据这个字段决定是继续检索还是结束进入综合回答。

### 10.6 LangGraph 和 main.py 的关系

用户执行：

```powershell
python main.py --ask "问题"
```

`main.py` 会调用：

```python
from agent.graph import run_agent

result = run_agent(
    args.ask,
    max_iterations=args.max_iterations,
    conversation_context=...,
    long_term_context=...,
)
```

`run_agent()` 内部会：

```python
app = build_agent_graph()
return app.invoke({
    "question": question,
    "max_iterations": max_iterations,
    "conversation_context": conversation_context,
    "long_term_context": long_term_context,
})
```

也就是说，`main.py` 只负责准备输入、注入记忆、保存结果；真正的 Agent 执行流程由 LangGraph 编排。

### 10.7 为什么不用简单函数顺序调用

这个项目当然也可以写成普通代码：

```python
plan = planner(question)
evidence = executor(plan)
decision = reasoner(evidence)
answer = synthesizer(evidence)
```

但这样做会有几个问题：

- 迭代流程会越来越难维护。
- 每个步骤之间的状态传递容易混乱。
- 条件分支不够清晰。
- 后续加节点、加工具、加更多分支时扩展性差。
- 不方便观察每一步的中间状态。

LangGraph 的优势是把这些变成显式图结构：

- 节点职责清楚。
- 状态字段清楚。
- 执行路径清楚。
- 条件循环清楚。
- 后续扩展更自然。

### 10.8 在项目中的具体价值

LangGraph 在这个项目中主要带来四个价值：

1. 流程结构化  
   把“规划、检索、判断、回答”拆成可维护的节点。

2. 状态统一管理  
   所有节点通过 `AgentState` 共享计划、证据、记忆和最终结果。

3. 支持条件循环  
   Reasoner 可以决定是否继续检索，形成迭代式 RAG。

4. 便于扩展  
   后续可以加入网页检索节点、工具选择节点、rerank 节点、图表解析节点，而不需要重写整个主流程。

### 10.9 面试版总结

可以这样回答：

> LangGraph 在这个项目里是 Agent 的工作流编排框架。项目用 `StateGraph(AgentState)` 定义了一个状态图，把 Agent 拆成 Planner、Executor、Reasoner 和 Synthesizer 四个节点。Planner 负责生成检索计划，Executor 通过 MCP 工具检索论文证据，Reasoner 判断证据是否充分，Synthesizer 生成最终回答。LangGraph 的关键作用是维护共享状态，并通过条件边实现循环：如果 Reasoner 设置 `needs_more_evidence=True`，流程会回到 Executor 继续检索；否则进入 Synthesizer 输出答案。因此这个项目不是简单的一次 RAG，而是一个可控的、可迭代的、带状态的论文研究 Agent。

## 11. MCP 是什么，为什么要用 MCP

### 11.1 MCP 是什么

MCP 全称是 Model Context Protocol，可以理解为“模型和外部工具/数据源之间的标准连接协议”。

它解决的问题是：Agent 想调用外部能力时，不应该每个工具都写一套私有调用方式。MCP 把工具、资源和上下文暴露成统一协议，让模型或 Agent 可以用标准方式发现工具、调用工具、拿到结构化结果。

简单类比：

```text
USB-C 统一了设备连接方式
MCP 统一了 Agent 连接外部工具和数据源的方式
```

在 Agent 项目里，MCP 通常负责把这些能力标准化暴露出来：

- 数据库查询。
- 本地文件检索。
- 向量库 RAG。
- 网页搜索。
- 代码执行。
- 企业系统 API。
- 自定义业务工具。

面试表达：

> MCP 是 Agent 和外部工具之间的标准协议。它把工具能力用统一接口暴露出来，让 Agent 不需要关心工具背后是数据库、搜索引擎、向量库还是本地脚本，只需要按协议调用工具并读取结果。

### 11.2 MCP 的基本角色

MCP 里通常有三个角色：

```text
MCP Host / Client  <->  MCP Server  <->  Tools / Resources
```

含义是：

- Host：运行 Agent 或模型的应用，比如一个 AI 助手、IDE、Agent 框架。
- Client：Host 内部负责和 MCP Server 通信的客户端。
- Server：对外暴露工具和资源的服务。
- Tools：真正执行动作的函数，比如搜索论文、查询数据库、调用网页搜索。
- Resources：可读取的数据资源，比如文件、文档、配置、数据库 schema。

这个项目里：

- `utils/mcp_client.py` 是 MCP client 辅助封装。
- `mcp_server/server.py` 是 MCP Server 入口。
- `mcp_server/tools/qdrant_rag.py` 暴露本地论文 RAG 工具。
- `mcp_server/tools/web_search.py` 暴露互联网研究搜索工具。

### 11.3 这个项目里的 MCP 是怎么用的

项目使用 FastMCP 创建本地 MCP Server：

```python
mcp = FastMCP(
    name="dse-explorer-mcp",
    instructions="Tools for searching a local DSE/MOO paper corpus..."
)
```

然后注册工具：

```python
register_qdrant_rag_tools(mcp)
register_web_search_tools(mcp)
```

当前注册的主要工具有：

```text
search_dse_papers
get_rag_corpus_stats
search_research_web
```

其中最核心的是 `search_dse_papers`，它内部调用 Qdrant Retriever，返回论文 chunk、相似度分数和 metadata。

Agent 的 `Executor` 节点并不直接调用 `QdrantRetriever`，而是调用：

```python
call_search_dse_papers_batch(requests)
```

这个函数在 `utils/mcp_client.py` 中会：

1. 用当前 Python 解释器启动本地 MCP Server 子进程。
2. 使用 stdio transport 和 Server 通信。
3. 初始化 MCP `ClientSession`。
4. 调用 `search_dse_papers` 工具。
5. 解码工具返回结果。
6. 把结构化结果交还给 Executor。

链路可以表示为：

```text
Executor
  -> utils/mcp_client.py
  -> 启动 mcp_server.server --transport stdio
  -> MCP ClientSession.call_tool("search_dse_papers", request)
  -> qdrant_rag.search_dse_papers()
  -> QdrantRetriever.search()
  -> 返回 evidence
```

面试表达：

> 在这个项目中，MCP 是 Agent 调用工具的中间层。Executor 不直接依赖 Qdrant 检索实现，而是通过 MCP client 调用 `search_dse_papers`。这样检索能力被封装成标准工具，后续可以被其他 Agent 或客户端复用。

### 11.4 为什么要 MCP

#### 统一工具调用协议

如果没有 MCP，每接一个工具都要写自己的调用方式：

```text
Qdrant 一套调用方式
Tavily 一套调用方式
SerpApi 一套调用方式
数据库一套调用方式
本地脚本一套调用方式
```

MCP 的价值是把它们统一成：

```text
call_tool(tool_name, arguments)
```

这样 Agent 只面向“工具名 + 参数 + 返回结果”编程。

#### 降低 Agent 和工具的耦合

没有 MCP 时，Agent 代码可能直接依赖具体工具实现：

```python
retriever = QdrantRetriever()
results = retriever.search(query)
```

这样 Executor 和 Qdrant 绑定得很死。如果以后把 Qdrant 换成 Milvus、Elasticsearch、Postgres pgvector，就可能要改 Agent 内部逻辑。

用了 MCP 后，Agent 只知道：

```text
我要调用 search_dse_papers
```

至于这个工具底层用 Qdrant、Milvus 还是别的系统，可以由 MCP Server 内部替换。

#### 工具可以独立发布和复用

MCP Server 可以独立运行。一个 MCP Server 暴露出的工具，不只这个项目能用，其他支持 MCP 的 Agent、IDE 或客户端也能用。

在这个项目中，论文检索能力被封装成 MCP 工具后，理论上可以被其他研究助手复用。

#### 更适合多工具 Agent

随着 Agent 能力变复杂，工具会越来越多。

例如后续可以加：

- 论文图表解析工具。
- BibTeX 生成工具。
- arXiv 检索工具。
- 实验代码执行工具。
- 数据库统计工具。
- rerank 工具。

如果每个工具都塞进 Agent 内部，代码会很乱。MCP 可以让工具层和 Agent 编排层分离。

#### 更利于权限和边界管理

MCP Server 是一个独立边界。它可以只暴露被允许的工具，而不是让 Agent 任意访问所有代码或系统资源。

例如这个项目的 MCP Server 暴露的是：

```text
search_dse_papers
get_rag_corpus_stats
search_research_web
```

Agent 只能通过这些工具拿到需要的信息，而不是随便操作整个向量库或文件系统。

### 11.5 如果不用 MCP 会怎么样

不用 MCP 也能做这个项目，但工程形态会不一样。

最直接的写法是让 `executor_node` 直接调用 Qdrant：

```python
retriever = QdrantRetriever()
results = retriever.search(query)
```

这样短期更简单，少了 MCP Server、ClientSession、stdio 子进程这些复杂度。

但缺点也明显：

- Agent 节点和具体工具实现强耦合。
- 工具难以被其他项目或客户端复用。
- 后续加入更多工具时，Executor 代码会越来越臃肿。
- 缺少统一的工具描述、参数 schema 和调用协议。
- 不方便把工具作为独立服务运行。
- 权限边界不如 MCP 清晰。
- 如果未来接入支持 MCP 的 IDE 或 Agent 平台，需要重新封装。

面试中可以比较平衡地说：

> 如果只是一个很小的 demo，不用 MCP、直接在代码里调用 Qdrant 也可以，甚至更简单。但这个项目是按 Agent 工程化方向搭的，MCP 的价值在于把检索、网页搜索等能力标准化成工具服务，降低 Agent 编排逻辑和工具实现之间的耦合，也方便后续扩展和复用。

### 11.6 MCP 和 LangGraph 的关系

LangGraph 和 MCP 解决的是不同层面的问题。

```text
LangGraph：负责 Agent 流程编排
MCP：负责 Agent 工具调用标准化
```

在这个项目中：

- LangGraph 决定什么时候调用 Executor，什么时候进入 Reasoner，什么时候结束。
- MCP 决定 Executor 如何以标准方式调用外部工具。

可以理解为：

```text
LangGraph 管“流程”
MCP 管“工具接口”
Qdrant 管“向量检索”
DeepSeek 管“语言理解和生成”
```

### 11.7 MCP 的通信方式

这个项目的 MCP Server 支持三种 transport：

```text
stdio
http
sse
```

当前 Agent Executor 使用的是 `stdio`。

stdio 模式下，Client 会启动一个本地 Server 子进程，然后通过标准输入输出和它通信。这适合本地工具和 IDE 场景，不需要额外启动 HTTP 服务。

项目里启动参数是：

```python
StdioServerParameters(
    command=sys.executable,
    args=["-m", "mcp_server.server", "--transport", "stdio"],
    cwd=str(PROJECT_ROOT),
)
```

如果要单独启动 MCP Server，也可以运行：

```powershell
python -m mcp_server.server --transport stdio
```

或者 HTTP：

```powershell
python -m mcp_server.server --transport http --host 127.0.0.1 --port 8765
```

### 11.8 面试版总结

可以这样回答：

> MCP 是 Model Context Protocol，是 Agent 连接外部工具和数据源的标准协议。这个项目用 FastMCP 实现了一个本地 MCP Server，把 Qdrant 论文检索、语料库统计、互联网研究搜索封装成工具。LangGraph 的 Executor 节点通过 MCP client 启动本地 MCP Server，并调用 `search_dse_papers` 获取论文证据。使用 MCP 的好处是工具调用标准化、Agent 和工具实现解耦、工具可以独立运行和复用，也方便后续扩展更多工具。如果不用 MCP，Executor 可以直接调用 Qdrant Retriever，短期更简单，但会让 Agent 逻辑和具体工具实现耦合更重，扩展多工具生态时维护成本更高。

## 12. 简历中如何描述这个项目

### 12.1 推荐项目名称

可以写成：

```text
dse-explorer：面向设计空间探索论文研究的 RAG Agent 系统
```

如果简历空间比较紧，可以写成：

```text
DSE 论文研究 RAG Agent
```

### 12.2 推荐项目简介

简历中的项目简介建议控制在 1-2 行：

> 构建面向设计空间探索（DSE）和多目标优化（MOO）研究场景的论文问答 Agent，基于本地 PDF 论文库、Qdrant 向量检索、LangGraph 工作流和 MCP 工具调用，实现论文证据驱动的多轮研究问答。

更偏工程化的版本：

> 设计并实现一个基于 LangGraph + MCP + Qdrant RAG 的论文研究 Agent，支持 PDF 论文入库、语义检索、证据评估、迭代检索、带来源回答和跨轮记忆。

### 12.3 推荐技术栈写法

可以放在项目标题后面：

```text
技术栈：Python, LangGraph, MCP/FastMCP, Qdrant, RAG, DeepSeek, DashScope Embedding, SQLite, pypdf
```

如果简历空间有限：

```text
Python / LangGraph / MCP / Qdrant / RAG / DeepSeek
```

### 12.4 推荐简历 bullet 版本

这一版适合直接放进简历：

```text
dse-explorer：面向 DSE/MOO 论文研究的 RAG Agent 系统
技术栈：Python, LangGraph, MCP/FastMCP, Qdrant, DeepSeek, DashScope Embedding, SQLite, pypdf

- 设计并实现面向设计空间探索（DSE）和多目标优化（MOO）论文研究的 RAG Agent，支持本地论文语义检索、证据驱动问答和多轮上下文记忆。
- 构建 PDF 入库流水线，使用 pypdf 逐页解析论文文本，按 chunk size + overlap 切分文本，调用 DashScope embedding 生成向量，并将文本、向量、页码和论文路径等 metadata 写入 Qdrant。
- 基于 LangGraph 搭建 Planner -> Executor -> Reasoner -> Synthesizer 的 Agent 工作流，实现检索计划生成、MCP 工具调用、证据充分性判断和最终答案综合。
- 使用 FastMCP 封装本地论文检索、语料库统计和互联网研究搜索工具，使 Agent 与底层 Qdrant、Tavily/SerpApi 等工具解耦，便于后续扩展。
- 设计 SQLite 会话记忆和长期记忆机制，保存历史问答、引用来源和高价值结论，支持跨轮问题中的指代解析和上下文延续。
```

### 12.5 偏算法 / Agent 方向写法

如果你投的是 Agent、LLM 应用、RAG 相关岗位，可以突出 Agent 范式：

```text
- 基于 LangGraph 实现 Plan-and-Execute + Reflection 的论文研究 Agent，将复杂研究问题拆解为检索计划，并通过 Reasoner 判断证据是否充分，必要时触发二次检索。
- 构建论文级 RAG 流程，将本地 DSE/MOO PDF 解析为带页码和路径 metadata 的向量知识库，支持回答时返回可追溯 citation。
- 通过 MCP 将 Qdrant 检索和 Web Search 抽象成标准工具，使 Agent 的流程编排与工具实现解耦，提高多工具扩展能力。
```

### 12.6 偏后端 / 工程方向写法

如果你投的是后端、平台、AI 工程化岗位，可以突出工程实现：

```text
- 实现 PDF 论文入库、解析缓存、批量 embedding、Qdrant upsert 和语义检索链路，支持 179 篇本地论文构建研究知识库。
- 使用 FastMCP 搭建本地 MCP Server，封装论文检索、语料库统计和网页搜索工具，并通过 stdio MCP client 在 Agent 执行阶段调用。
- 设计配置管理、日志、异常隔离和 SQLite 本地记忆模块，提升系统可调试性和多轮问答连续性。
```

### 12.7 精简版写法

如果简历项目很多，只能写 3 条：

```text
- 基于 LangGraph + Qdrant + DeepSeek 构建 DSE/MOO 论文研究 RAG Agent，支持本地论文检索、证据驱动回答和来源页码输出。
- 实现 PDF 入库流程：pypdf 文本抽取、chunk 切分、DashScope embedding、Qdrant 向量写入，并保存论文路径/页码 metadata。
- 使用 MCP/FastMCP 将论文检索和网页搜索封装为标准工具，并结合 SQLite 记忆机制支持多轮研究问答。
```

### 12.8 面试追问时可以强调的亮点

如果面试官问“这个项目难点是什么”，可以说：

```text
难点不只是把文档放进向量库，而是把论文问答做成一个可控的 Agent 流程：
1. Planner 先把问题拆成检索任务。
2. Executor 通过 MCP 工具检索论文证据。
3. Reasoner 判断证据是否足够，不够就继续检索。
4. Synthesizer 基于证据生成带来源的回答。
5. SQLite 记忆模块支持跨轮上下文。
```

如果面试官问“为什么用 LangGraph 和 MCP”，可以说：

```text
LangGraph 解决流程编排问题，让 Agent 从简单的一次 RAG 变成可迭代的状态机；
MCP 解决工具调用标准化问题，让论文检索、网页搜索等能力与 Agent 编排逻辑解耦。
```

如果面试官问“项目有什么不足”，可以坦诚说：

```text
当前系统主要处理文本型 PDF，对扫描版 PDF、复杂表格、公式和图像信息支持有限；
Agent 的工具选择策略还比较固定，后续可以加入更灵活的 tool router、reranker 和图表解析模块。
```

### 12.9 最推荐放入简历的版本

如果只能选一版，推荐这样写：

```text
dse-explorer：面向 DSE/MOO 论文研究的 RAG Agent 系统
技术栈：Python, LangGraph, MCP/FastMCP, Qdrant, DeepSeek, DashScope Embedding, SQLite

- 构建面向设计空间探索和多目标优化研究的论文问答 Agent，支持本地论文语义检索、证据驱动回答、来源页码输出和多轮上下文记忆。
- 实现 PDF 论文入库流水线，使用 pypdf 逐页解析文本，按 chunk size + overlap 切分，调用 DashScope embedding 生成向量，并将文本、向量和 metadata 写入 Qdrant。
- 基于 LangGraph 设计 Planner -> Executor -> Reasoner -> Synthesizer 工作流，通过证据充分性判断实现迭代检索，而不是简单的一次性 RAG。
- 使用 MCP/FastMCP 封装 Qdrant 论文检索、语料库统计和 Web Search 工具，降低 Agent 编排逻辑与底层工具实现的耦合。
```

## 13. 上下文是怎么实现的，技术点在哪

### 13.1 这个项目里的“上下文”指什么

这个项目里的上下文主要有三类：

```text
1. 当前问题上下文：用户本轮输入的问题
2. 短期会话上下文：最近几轮问答的摘要和来源
3. 长期记忆上下文：从历史问答中抽取出的高价值结论
```

此外还有一类 RAG 上下文：

```text
4. 检索证据上下文：从 Qdrant 找到的论文 chunk，被压缩后交给 Reasoner 和 Synthesizer
```

所以这个项目不是依赖 LLM 自己“记住之前说过什么”，而是把历史信息显式保存、检索、拼接进 prompt。

面试表达：

> 项目的上下文是显式实现的，不是靠模型会话天然记忆。系统会把历史问答保存到 SQLite，每次提问前根据 session_id 构造短期会话上下文和长期记忆上下文，再注入 LangGraph 的 AgentState，最终进入 Planner、Reasoner 和 Synthesizer 的 prompt。

### 13.2 整体链路

一次 `--ask` 的上下文链路是：

```text
用户提问
-> main.py 创建 MemoryStore
-> 根据 session_id 和当前 query 构造 MemoryContext
-> MemoryContext 注入 run_agent()
-> LangGraph 把 conversation_context / long_term_context 放进 AgentState
-> Planner / Reasoner / Synthesizer 构造 prompt 时读取这些上下文
-> Agent 生成回答
-> 回答结束后 save_turn() 保存本轮问答
-> LLM 总结本轮问答并抽取长期记忆
-> 写入 SQLite
```

对应代码位置：

- `main.py`：负责读取和保存上下文。
- `utils/memory.py`：负责 SQLite 记忆存储、检索、摘要和长期记忆抽取。
- `agent/state.py`：在 `AgentState` 中定义上下文字段。
- `agent/prompts.py`：把上下文注入 Planner、Reasoner、Synthesizer 的 prompt。
- `agent/graph.py`：把上下文作为初始状态传入 LangGraph。

### 13.3 短期会话上下文怎么实现

短期会话上下文来自 SQLite 表：

```text
conversation_turns
```

表里保存：

```text
id
session_id
created_at
question
answer
sources_json
summary
```

每次提问前，`MemoryStore.get_recent_conversation_context()` 会按 `session_id` 查询最近几轮问答：

```sql
SELECT question, answer, sources_json, summary, created_at
FROM conversation_turns
WHERE session_id = ?
ORDER BY id DESC
LIMIT ?
```

默认注入最近 3 轮，由参数控制：

```powershell
--memory-turns 3
```

构造出来的上下文不是完整历史回答，而是：

```text
Turn 1:
User: 上一轮问题
Summary: 上一轮问答摘要
Sources: 引用来源
```

这样做可以控制 prompt 长度，避免把完整历史回答全部塞进去。

面试表达：

> 短期上下文不是简单拼接完整历史，而是保存每轮问答的摘要和引用来源。下一轮只注入最近 N 轮摘要，这样既能处理“它”“这个方法”等指代，又能控制 token 成本。

### 13.4 长期记忆怎么实现

长期记忆来自 SQLite 表：

```text
long_term_memories
```

表里保存：

```text
id
session_id
created_at
kind
content
source
importance
```

其中 `kind` 有三类：

```text
conclusion
user_focus
paper_summary
```

每轮问答结束后，系统会调用 `extract_long_term_memories()`，让 LLM 从本轮问答中抽取值得长期保存的信息。

例如：

```text
GRL-DSE 的核心方法是什么
用户关注的是 DSE 优化方法比较
某篇论文的关键结论
```

这些信息会被保存到 `long_term_memories` 表里。

为了避免重复保存，写入前会检查同一个 `session_id + kind + content` 是否已经存在。

### 13.5 长期记忆如何检索

每次新问题进来时，`search_long_term_context()` 会先取出当前 session 和 global session 的候选长期记忆：

```sql
SELECT kind, content, source, importance, created_at
FROM long_term_memories
WHERE session_id IN (?, 'global')
ORDER BY importance DESC, id DESC
LIMIT 200
```

然后用一个轻量 token overlap 方法和当前 query 做相关性匹配：

```text
score = overlap + importance
```

其中：

- `overlap`：当前问题和记忆内容的 token 交集数量。
- `importance`：记忆重要性分数。

最后取前 5 条拼成长期记忆上下文。

这里没有用向量检索长期记忆，而是用了关键词 overlap + importance 的轻量检索方式。

面试表达：

> 长期记忆没有再单独建向量库，而是存 SQLite 后用 token overlap 加 importance 做轻量检索。这个实现简单、成本低，适合当前项目规模。后续如果记忆量变大，可以替换成向量化 memory retrieval。

### 13.6 上下文如何注入 Agent

`main.py` 中构造好上下文后，会调用：

```python
result = run_agent(
    args.ask,
    max_iterations=args.max_iterations,
    conversation_context=memory_context.conversation_context,
    long_term_context=memory_context.long_term_context,
)
```

`run_agent()` 会把它们放入 LangGraph 初始状态：

```python
app.invoke({
    "question": question,
    "max_iterations": max_iterations,
    "conversation_context": conversation_context,
    "long_term_context": long_term_context,
})
```

在 `AgentState` 中，这两个字段是：

```python
conversation_context: str
long_term_context: str
```

之后每个节点都可以从 state 中读取。

### 13.7 上下文注入到哪些 Prompt

项目把上下文注入了三个关键 prompt。

#### Planner

Planner prompt 中包含：

```text
Recent conversation context:
...

Relevant long-term memory:
...

Current user question:
...
```

作用是让 Planner 能解析指代，并生成更准确的检索 query。

例如用户第二轮问：

```text
它用了什么优化方法？
```

Planner 可以根据上一轮上下文知道“它”指的是 `GRL-DSE`。

#### Reasoner

Reasoner prompt 中也包含：

```text
Recent conversation context
Relevant long-term memory
Compressed retrieved evidence
```

作用是判断证据是否足够时，不只看本轮问题，还能参考历史关注点。

#### Synthesizer

Synthesizer prompt 中包含：

```text
User question
Recent conversation context
Relevant long-term memory
Retrieved evidence
```

作用是最终回答时能保持多轮连续性，同时仍然基于检索证据回答。

### 13.8 RAG 证据上下文怎么处理

除了记忆上下文，项目还有一层检索证据上下文。

Executor 检索得到 evidence 后，Reasoner 会调用 `compress_evidence()`，把多个检索结果压缩成文本块：

```text
- Evidence label: [source, page]
  Task: ...
  Score: ...
  Text: ...
```

它会做几件事：

- 去重：避免相同来源、相同页码、相似文本重复出现。
- 排序：按检索分数从高到低排序。
- 截断：每个结果限制字符数。
- 限量：默认最多保留前 10 个结果。

压缩后的 `compressed_context` 会给 Reasoner 判断证据是否足够，也会给 Synthesizer 生成最终答案。

面试表达：

> 项目里有两类上下文：memory context 解决多轮对话连续性，retrieved evidence context 解决回答依据问题。Reasoner 会压缩检索证据，既控制 token，又保留 source、page、score，方便最后输出 citation。

### 13.9 回答后如何保存上下文

Agent 生成最终回答后，`main.py` 会调用：

```python
memory_store.save_turn(
    session_id=args.session,
    question=args.ask,
    answer=result.get("final_answer", ""),
    sources=sources,
)
```

`save_turn()` 做两件事：

1. 保存本轮会话到 `conversation_turns`。
2. 抽取长期记忆并保存到 `long_term_memories`。

保存会话前，会调用 `summarize_turn()`，用 LLM 把本轮问答压缩成 2-3 句话摘要。

抽取长期记忆时，会调用 `extract_long_term_memories()`，要求 LLM 返回严格 JSON：

```json
{
  "memories": [
    {
      "kind": "conclusion|user_focus|paper_summary",
      "content": "一条可复用的中文记忆",
      "source": "论文或会话来源",
      "importance": 0.8
    }
  ]
}
```

如果 LLM 摘要或抽取失败，代码会降级使用 fallback summary，保证流程不中断。

### 13.10 可控制参数

项目提供了几个上下文相关参数：

```powershell
--session demo
```

指定会话 ID。不同 session 的记忆隔离。

```powershell
--memory-turns 3
```

控制注入最近几轮短期会话。

```powershell
--no-memory
```

禁用记忆上下文，只做本轮问答。

```powershell
--show-memory
```

打印本轮注入的短期上下文和长期记忆，方便调试。

```powershell
--show-trace
```

打印 Agent 的计划、压缩证据、Reasoner 判断和 evidence。

### 13.11 技术点总结

这个项目的上下文技术点主要有：

```text
1. SQLite 持久化记忆
2. session_id 会话隔离
3. 最近 N 轮会话摘要注入
4. LLM 自动摘要历史问答
5. LLM 自动抽取长期记忆
6. 长期记忆按 token overlap + importance 检索
7. LangGraph AgentState 显式传递上下文字段
8. Planner / Reasoner / Synthesizer 多节点 prompt 注入
9. RAG evidence 压缩、去重、排序和截断
10. 支持 --no-memory / --show-memory / --memory-turns 调试和控制
```

### 13.12 如果面试官问“上下文窗口不够怎么办”

可以这样回答：

> 项目没有把全部历史对话直接塞进 prompt，而是做了两层压缩。短期上下文只注入最近 N 轮的摘要和来源；长期上下文只检索与当前问题相关的高价值记忆；检索证据也会去重、按分数排序并截断。因此它是在应用层做 context management，而不是完全依赖模型上下文窗口。

### 13.13 当前实现的不足和优化方向

当前实现比较轻量，适合项目规模，但还有优化空间：

- 长期记忆检索目前是 token overlap，不是向量检索。
- 记忆重要性主要由 LLM 抽取时给出，缺少后续动态更新。
- 没有复杂的 memory decay 或 memory consolidation。
- 对冲突记忆没有做显式解决。
- 没有单独的上下文 rerank 模块。

可以改进为：

- 把长期记忆也向量化，放入 Qdrant 或 SQLite vector extension。
- 加入 memory reranker。
- 根据用户反馈更新 importance。
- 定期合并相似长期记忆。
- 对 prompt context 做 token budget 管理。

### 13.14 面试版总结

可以这样回答：

> 这个项目的上下文是应用层显式管理的。每轮问答结束后，系统把问题、回答、引用来源和摘要保存到 SQLite，同时用 LLM 从回答中抽取长期记忆。下一轮提问时，系统根据 session_id 取最近 N 轮会话摘要作为短期上下文，再根据当前 query 从长期记忆中检索相关内容，然后把 `conversation_context` 和 `long_term_context` 注入 LangGraph 的 AgentState。Planner、Reasoner 和 Synthesizer 构造 prompt 时都会读取这些上下文，从而支持多轮指代解析和连续研究问答。技术点包括 SQLite 持久化、session 隔离、LLM 摘要、长期记忆抽取、轻量相关性检索、状态传递和 prompt 注入。

## 14. 如何把项目做得更复杂、更有亮点

当前项目已经有基础 RAG、LangGraph Agent、MCP 工具、记忆机制，但整体还偏“论文问答 demo”。如果想让它更像一个完整的 AI research assistant，可以从下面几个方向增强。

### 14.1 优先级最高的增强方向

最推荐优先做这 4 类：

```text
1. 提升 RAG 检索质量：Hybrid Search + Rerank + Query Rewrite
2. 提升论文理解能力：自动抽取论文元数据、方法、指标、贡献
3. 提升 Agent 复杂度：Tool Router + 多步研究任务 + 报告生成
4. 增加评估体系：检索质量、引用准确性、回答可信度评测
```

这几类最适合写进简历，因为它们不是简单加页面，而是体现 RAG、Agent、工程化和评估能力。

### 14.2 RAG 检索增强

当前项目主要是 dense vector search。可以升级为更完整的 RAG 检索系统。

#### Hybrid Search

加入关键词检索，例如 BM25，与向量检索融合。

```text
Dense Retrieval：擅长语义相似
BM25：擅长精确术语、方法名、论文名
Hybrid Search：两者融合
```

适合 DSE 论文场景，因为很多问题会包含具体方法名：

```text
GRL-DSE
BOOM-Explorer
ArchGym
Bayesian Optimization
NSGA-II
```

只用向量检索时，精确方法名可能不如 BM25 稳。

#### Reranker

第一阶段召回 top 30 或 top 50，然后用 reranker 重新排序，取 top 5 给 LLM。

可以使用：

- BGE reranker。
- Cohere rerank。
- 自己用 LLM 做 evidence rerank。

面试亮点：

> 我把 RAG 拆成召回和重排序两阶段，先用 hybrid search 提高召回，再用 reranker 提高证据精度。

#### Query Rewrite

在检索前让 LLM 把用户问题改写成更适合论文检索的英文 query。

例如：

```text
用户：它用了什么优化方法？
改写：GRL-DSE graph reinforcement learning design space exploration optimization method
```

当前 Planner 已经有类似能力，可以进一步独立成 Query Rewriter 节点。

#### Metadata Filter

利用 metadata 做过滤：

- 按论文类别过滤。
- 按年份过滤。
- 按文件名过滤。
- 按页码范围过滤。
- 按会议过滤。

前提是先把论文元数据抽取出来。

### 14.3 PDF 和论文解析增强

当前项目用 pypdf 抽取纯文本，复杂论文结构支持有限。可以增强论文解析能力。

#### OCR 支持

对扫描版 PDF 加 OCR：

- PaddleOCR。
- Tesseract。
- MinerU / marker 类工具。

这样可以处理扫描论文和图片文字。

#### 表格和公式处理

DSE 论文经常有结果表、指标表、对比表。可以加入表格解析：

- 提取表格为 Markdown。
- 保存表格页码和 caption。
- 让问答可以引用表格。

公式可以先做轻量处理：

- 提取公式附近文本。
- 保留公式所在页码。
- 不一定一开始就做完整 LaTeX 还原。

#### 论文元数据抽取

对每篇论文抽取：

```text
title
authors
year
venue
abstract
keywords
method name
task
objective
baseline
dataset / benchmark
metrics
main contribution
limitations
```

这些信息可以存到 SQLite 或单独 JSON 中，也可以作为 Qdrant payload。

面试亮点：

> 我不仅把论文切 chunk 入库，还抽取结构化元数据，使系统能按年份、会议、方法类别、优化目标进行检索和分析。

### 14.4 知识图谱 / 方法图谱

如果想让项目更有研究味，可以构建一个 DSE 方法知识图谱。

节点可以包括：

```text
Paper
Method
Optimization Algorithm
Benchmark
Objective
Metric
Architecture
Dataset
```

边可以包括：

```text
paper proposes method
method uses algorithm
method optimizes objective
method evaluates on benchmark
method compares with baseline
paper cites paper
```

用途：

- 查某类方法的发展脉络。
- 比较不同 DSE 方法。
- 生成 survey。
- 找相似论文。
- 分析趋势。

面试亮点：

> 在 RAG 之外，我还把论文中的方法、目标、算法、benchmark 抽取成知识图谱，用于支持结构化分析和综述生成。

### 14.5 Agent 能力增强

当前 Agent 是固定流程：

```text
Planner -> Executor -> Reasoner -> Synthesizer
```

可以升级为更灵活的研究 Agent。

#### Tool Router

新增一个 Tool Router 节点，让 Agent 判断该用哪个工具：

```text
本地论文检索
网页搜索
语料库统计
代码执行
论文元数据查询
知识图谱查询
```

这样就不是 Executor 固定调用 `search_dse_papers`，而是根据任务选择工具。

#### 多 Agent 协作

可以拆成多个角色：

```text
Planner Agent：拆解研究任务
Retriever Agent：检索论文证据
Reader Agent：阅读和压缩论文
Critic Agent：检查证据是否充分
Writer Agent：生成最终报告
```

这会让项目更接近复杂 Agent 系统。

#### 长任务研究模式

支持用户给一个大任务：

```text
请总结近几年 LLM for DSE 的主要方法，并比较它们和传统 BO/RL 方法的区别。
```

Agent 自动执行：

```text
生成研究计划
检索多篇论文
抽取每篇论文方法
构建对比表
总结趋势
输出 Markdown 报告
保存报告
```

#### 报告生成

增加 report generator：

- 生成 Markdown survey。
- 生成论文对比表。
- 生成方法 taxonomy。
- 生成引用列表。
- 导出为 PDF 或 docx。

这非常适合面试展示。

### 14.6 DSE 领域能力增强

为了让项目不只是通用 RAG，可以加入 DSE 领域特化能力。

#### 方法分类器

自动把论文分成：

```text
Bayesian Optimization
Reinforcement Learning
Graph Learning
Evolutionary Algorithm
LLM-based DSE
Surrogate Model
Multi-objective Optimization
Cross-workload DSE
```

#### DSE 方法对比矩阵

自动生成对比表：

```text
方法 | 优化算法 | 设计变量 | 优化目标 | 评估平台 | 优点 | 局限
```

#### Benchmark / Metric 抽取

抽取论文里的：

```text
performance
power
area
energy
latency
throughput
Pareto optimality
simulation time
sample efficiency
```

#### 研究趋势分析

按年份分析：

- 传统 BO/RL 方法如何发展。
- LLM-DSE 方法什么时候出现。
- 哪些 benchmark 常用。
- 哪些目标函数最常见。

面试亮点：

> 我把通用 RAG 系统进一步结合 DSE 领域知识，支持方法分类、优化目标抽取和论文对比矩阵生成。

### 14.7 Web UI / 产品化增强

可以做一个简单 Web UI，让项目更容易展示。

推荐功能：

- 上传 PDF。
- 查看入库状态。
- Chat with papers。
- 显示引用来源。
- 点击来源跳转到 PDF 页。
- 展示检索证据。
- 展示 Agent trace。
- 管理 session。
- 生成并下载研究报告。

技术选型可以是：

```text
FastAPI + React
```

或者更快：

```text
Streamlit / Gradio
```

面试时，哪怕 UI 简单，也能显著提升项目完整度。

### 14.8 评估体系增强

这是很多 RAG 项目缺失的部分，做了会很加分。

可以建立一个小型 benchmark：

```text
问题
标准答案
相关论文
相关页码
期望引用
```

评估指标：

- Retrieval Recall@K。
- MRR / nDCG。
- 引用页码是否正确。
- 回答是否 grounded。
- 是否出现 hallucination。
- 答案完整度。

还可以记录：

- 每次 query。
- 检索结果。
- rerank 结果。
- prompt。
- LLM 输出。
- token 消耗。
- latency。

面试亮点：

> 我没有只做功能，还为 RAG 建了评估集，用 Recall@K、引用准确率和回答 groundedness 来衡量系统质量。

### 14.9 工程化增强

如果想让项目更像生产系统，可以做：

- Docker Compose：启动 Qdrant、后端服务、前端。
- FastAPI 后端：提供问答、入库、检索、报告接口。
- Celery / RQ：异步处理 PDF 入库。
- 数据库 migration：管理 SQLite 或 Postgres schema。
- 配置分环境：dev/test/prod。
- 单元测试和集成测试。
- CI：自动运行测试和 lint。
- 日志和 trace：记录每个 Agent 节点耗时。
- 缓存：缓存 embedding、检索结果、LLM 输出。

这些更适合后端或 AI 工程化岗位。

### 14.10 推荐实际开发路线

不要一次性做太多。建议按三阶段推进。

#### 第一阶段：增强 RAG 质量

优先实现：

```text
Hybrid Search
Reranker
Query Rewrite
RAG 评估集
```

这一步最能体现 RAG 深度。

#### 第二阶段：增强论文理解

实现：

```text
论文元数据抽取
方法分类
DSE 方法对比表
报告生成
```

这一步能体现领域理解。

#### 第三阶段：增强产品和工程化

实现：

```text
Web UI
异步入库
Agent trace 可视化
Docker 部署
测试和评估面板
```

这一步能体现完整项目能力。

### 14.11 最建议你做的 5 个功能

如果只选 5 个，建议做：

```text
1. Hybrid Search + Reranker
2. 论文元数据和方法信息自动抽取
3. DSE 方法对比矩阵生成
4. 长任务 Survey 报告生成
5. RAG 评估集和 Agent trace 可视化
```

这 5 个做完后，简历可以从“一个 RAG Agent demo”升级成：

```text
一个面向 DSE 领域的论文研究智能体系统，具备文献入库、混合检索、证据重排、结构化论文理解、多步研究规划、综述生成和评估闭环。
```

### 14.12 面试版总结

可以这样说：

> 当前项目已经实现了基础的论文 RAG Agent，包括 PDF 入库、Qdrant 检索、LangGraph 编排、MCP 工具和记忆机制。下一步我会从三个方向增强：第一是提升 RAG 质量，引入 hybrid search、reranker 和 query rewrite；第二是增强论文理解能力，自动抽取论文元数据、方法、优化目标和 benchmark，并生成 DSE 方法对比矩阵；第三是增强 Agent 和工程化能力，支持长任务研究、自动生成 survey 报告、Agent trace 可视化和 RAG 评估体系。这样项目就不只是问答 demo，而是一个面向 DSE 研究的完整 AI research assistant。

## 15. Hybrid Search、Reranker、Query Rewrite、RAG 评估集分别是什么，当前项目做到哪一步

### 15.1 当前项目里的 RAG 是什么形态

当前项目已经实现的是一个基础的 dense vector RAG。

整体链路是：

```text
用户问题
-> Planner 生成 semantic query
-> Executor 通过 MCP 调用 search_dse_papers
-> QdrantRetriever 把 query 转成 embedding
-> Qdrant 按向量相似度检索 top_k chunk
-> Reasoner 判断证据是否足够
-> Synthesizer 基于证据回答
```

对应代码：

- `agent/nodes/planner.py`：生成检索计划和 query。
- `agent/nodes/executor.py`：执行检索计划。
- `utils/mcp_client.py`：通过 MCP 调用本地检索工具。
- `mcp_server/tools/qdrant_rag.py`：暴露 `search_dse_papers`。
- `rag_pipeline/retriever.py`：调用 Qdrant 做向量检索。
- `utils/embeddings.py`：把 query 和文档 chunk 转成 embedding。

当前检索方式本质上是：

```text
query embedding 和 document chunk embedding 做相似度匹配
```

也就是 dense retrieval。

### 15.2 当前项目已经有的能力

当前已经有：

```text
1. PDF 文本解析和 chunk 切分
2. Embedding 生成
3. Qdrant 向量库
4. Dense vector search
5. Planner 生成检索 query
6. Reasoner 判断证据是否足够
7. 证据压缩和来源页码输出
8. 多轮上下文记忆
```

本次增强后仍未加入 RAG 评估集，但已经补上：

```text
1. BM25 / 关键词检索
2. Hybrid Search
3. 独立 Query Rewrite 节点
4. Reranker 二阶段重排
```

当前仍然没有：

```text
1. 系统化 RAG 评估集
```

### 15.3 Query Rewrite 是什么

Query Rewrite 是“检索前的问题改写”。

用户的问题往往不适合直接拿去做向量检索。例如：

```text
它用了什么优化方法？
```

这个问题里有“它”，没有明确论文名，也没有英文技术关键词。直接检索效果可能不好。

Query Rewrite 会把它改写成更适合检索的 query：

```text
GRL-DSE graph reinforcement learning design space exploration optimization method
```

Query Rewrite 通常会做几件事：

- 消解指代：把“它”“这个方法”替换成具体方法名。
- 中英文转换：把中文问题改写成英文技术检索词。
- 补充同义词：如 DSE -> design space exploration。
- 提取关键词：方法名、优化算法、任务、指标。
- 生成多个检索 query。

当前项目里 Planner 之前已经承担了一部分 Query Rewrite 的功能。

例如 `planner_node` 会调用 LLM，根据用户问题生成：

```json
{
  "steps": [
    {
      "task": "what to investigate",
      "query": "semantic search query"
    }
  ]
}
```

现在已经新增独立的 `query_rewriter_node`，放在 Planner 和 Executor 之间：

```text
User Question + Memory
-> Query Rewriter
-> 生成 rewritten_query 和 query_variants
-> Hybrid Retriever
```

面试表达：

> 当前项目现在有独立 Query Rewriter。Planner 先生成检索计划，Query Rewriter 再结合用户问题和记忆上下文，把每个检索任务改写成更适合论文检索的英文 rewritten_query，并生成多个 query_variants，用于后续 hybrid retrieval。

### 15.4 Hybrid Search 是什么

Hybrid Search 是“混合检索”，通常指：

```text
Dense Vector Search + Sparse Keyword Search
```

Dense Vector Search：

- 用 embedding 做语义匹配。
- 优点：能找语义相关内容。
- 缺点：对精确术语、论文名、缩写有时不稳定。

Sparse Keyword Search：

- 常见算法是 BM25。
- 根据关键词重合、词频、逆文档频率打分。
- 优点：对方法名、论文名、公式名、缩写很准。
- 缺点：不理解深层语义。

Hybrid Search 把两者结合：

```text
向量检索召回一批结果
BM25 关键词检索召回一批结果
结果融合排序
```

例如用户问：

```text
BOOM-Explorer 和 GRL-DSE 有什么区别？
```

BM25 很容易命中包含 `BOOM-Explorer`、`GRL-DSE` 的 chunk；向量检索能补充语义相关内容。两者结合比单独向量检索更稳。

当前项目已经新增 Hybrid Search。`rag_pipeline/retriever.py` 现在包含：

```text
Dense Retrieval: Qdrant 向量召回
Sparse Retrieval: 内存 BM25 关键词召回
Fusion: Reciprocal Rank Fusion, RRF
```

融合方式使用 RRF：

- Reciprocal Rank Fusion, RRF：根据排名融合，比直接合并分数更稳。

面试表达：

> 当前项目已经从单纯 dense vector retrieval 升级成 Hybrid Search：用 Qdrant 向量检索保证语义召回，用 BM25 稀疏关键词检索保证方法名、论文名等精确匹配，再通过 RRF 做结果融合。

### 15.5 Reranker 是什么

Reranker 是“二阶段重排序”。

典型 RAG 检索分两步：

```text
第一阶段：Retriever 快速召回 top 30 / top 50
第二阶段：Reranker 精排，选出最相关的 top 5 / top 10
```

Retriever 追求快和召回率，Reranker 追求精度。

Reranker 的输入通常是：

```text
query + candidate chunk
```

输出是每个 chunk 和 query 的相关性分数。

可以使用：

- Cross-encoder reranker。
- BGE reranker。
- Cohere rerank。
- Jina reranker。
- LLM-as-reranker。

当前项目已经新增轻量 Reranker。链路现在是：

```text
Qdrant / Hybrid Search 召回 top 30
-> Lightweight Reranker 重新计算相关性
-> 选择 top 5 evidence
-> Reasoner / Synthesizer
```

当前 reranker 不依赖外部 cross-encoder 服务，而是结合：

```text
RRF fused score
query token overlap
方法名 / 缩写 exact match
source file match
phrase hit
```

计算 `rerank_score`，再排序。

面试表达：

> 当前项目已经加入二阶段 rerank。第一阶段先做 dense + BM25 的 hybrid recall，第二阶段用 lightweight reranker 根据融合分数、关键词重合、方法名命中和来源命中重新排序，减少无关 evidence 进入 LLM。

### 15.6 RAG 评估集是什么

RAG 评估集是用来衡量系统检索和回答质量的一组标准问题。

它通常包含：

```text
question: 用户问题
gold_answer: 标准答案或参考答案
gold_sources: 应该命中的论文
gold_pages: 应该引用的页码
gold_chunks: 相关 chunk id
expected_keywords: 答案中应包含的关键点
```

评估可以分两层。

#### 检索评估

看系统有没有把正确证据找出来。

常用指标：

```text
Recall@K：正确证据是否出现在 top K
Precision@K：top K 里有多少是相关证据
MRR：第一个正确结果排在多前
nDCG：排序质量
```

#### 生成评估

看最终回答是否可靠。

可以评估：

```text
答案是否覆盖关键点
引用是否正确
是否基于 evidence
是否幻觉
是否回答了问题
```

当前项目没有系统化 RAG 评估集。

当前只能通过人工提问和 `--show-trace` 看检索证据，没有自动打分。

如果要增强，可以新建：

```text
eval/
  datasets/dse_qa.jsonl
  run_retrieval_eval.py
  run_answer_eval.py
  metrics.py
```

一个样例数据可以是：

```json
{
  "question": "GRL-DSE 的核心思想是什么？",
  "gold_sources": ["DSE/DAC23_GRL_DSE.pdf"],
  "gold_pages": [1, 2],
  "expected_keywords": ["graph representation learning", "reinforcement learning", "design space exploration"]
}
```

面试表达：

> 当前项目有 trace，可以人工看每一步检索和回答，但还没有评估闭环。后续可以建设 RAG benchmark，用 Recall@K、MRR、引用准确率和 groundedness 来衡量检索和回答质量。

### 15.7 四者在完整 RAG 中的关系

增强后的完整链路现在是：

```text
User Question
-> Query Rewrite
-> Dense Retrieval
-> BM25 Retrieval
-> Hybrid Fusion
-> Reranker
-> Evidence Compression
-> Reasoner
-> Synthesizer
```

对比增强前项目：

```text
User Question
-> Planner 生成 query
-> Dense Retrieval
-> Evidence Compression
-> Reasoner
-> Synthesizer
```

也就是说：

```text
增强前：基础 dense RAG + Agent 证据判断
增强后：Query Rewrite + 混合召回 + 二阶段精排 + Agent 证据判断
```

### 15.8 面试版总结

可以这样回答：

> 当前项目已经从基础 dense RAG 升级为 Query Rewrite + Hybrid Search + Reranker 的检索链路。Planner 生成检索计划后，Query Rewriter 会结合上下文生成 rewritten_query 和 query_variants；Retriever 同时执行 Qdrant dense retrieval 和 BM25 sparse retrieval，并用 RRF 融合候选结果；随后 lightweight reranker 根据融合分数、关键词重合、方法名命中等信号重排 evidence。系统仍未加入 RAG 评估集，后续如果继续增强，可以再补 Recall@K、MRR、引用准确率等自动评估。

## 16. 论文元数据和方法信息自动抽取

### 16.1 这个功能是什么

论文元数据和方法信息自动抽取，是把 PDF 论文从“非结构化文本”进一步转成“结构化研究记录”。

之前的 RAG 入库主要保存：

```text
chunk text
embedding vector
source path
page
chunk_index
```

这适合问答检索，但不适合直接做论文统计、方法分类、横向对比和 Survey 报告生成。

新增的自动抽取能力会读取论文前几页文本，调用 LLM 抽取：

```text
metadata: 论文基本信息
method: 方法和实验信息
tags: 领域标签
```

并保存到本地 SQLite：

```text
rag_pipeline/data/paper_info.sqlite3
```

对应模块：

```text
rag_pipeline/paper_info.py
```

### 16.2 “元数据”具体指什么

元数据是论文的基本出版和描述信息，主要回答“这篇论文是谁写的、叫什么、发表在哪里、主题是什么”。

当前抽取字段包括：

```text
title: 论文标题
authors: 作者列表
year: 发表年份
venue: 会议、期刊或来源
abstract: 摘要
keywords: 关键词
```

例子：

```json
{
  "title": "Graph Representation Learning for Microarchitecture Design Space Exploration",
  "authors": ["Xiaoling Yi", "Jialin Lu", "Xiankui Xiong"],
  "year": "2023",
  "venue": "DAC",
  "abstract": "This work presents GRL-DSE...",
  "keywords": ["design space exploration", "graph representation learning", "microarchitecture"]
}
```

这些字段后续可以用于：

- 按年份筛选论文。
- 按会议筛选论文。
- 快速展示论文卡片。
- 生成参考文献列表。
- 做研究趋势统计。

### 16.3 “方法信息”具体指什么

方法信息是论文的技术内容摘要，主要回答“这篇论文提出了什么方法、解决什么问题、怎么做实验、效果如何”。

当前抽取字段包括：

```text
method_name: 方法名
problem: 解决的问题
core_idea: 核心思想
optimization_algorithm: 使用的优化算法
model_or_agent: 使用的模型、代理模型或 Agent
design_variables: 设计变量
objectives: 优化目标
benchmarks: benchmark 或评估平台
baselines: 对比方法
metrics: 评价指标
main_results: 主要实验结果
limitations: 局限性
```

以 GRL-DSE 为例，理想抽取结果类似：

```json
{
  "method_name": "GRL-DSE",
  "problem": "microarchitecture design space exploration",
  "core_idea": "Use graph representation learning to build a compact continuous graph embedding space, then perform multi-objective Bayesian optimization.",
  "optimization_algorithm": "multi-objective Bayesian optimization",
  "model_or_agent": "graph neural network and ensemble surrogate model",
  "design_variables": ["microarchitecture configuration parameters"],
  "objectives": ["performance", "power", "area"],
  "benchmarks": ["BOOM design space", "ICCAD CAD Contest 2022 platform"],
  "baselines": ["BOOM-Explorer", "bare Bayesian optimization"],
  "metrics": ["hypervolume", "GD"],
  "main_results": ["Improves design space exploration quality over BOOM-Explorer"],
  "limitations": []
}
```

这些字段后续可以用于：

- 生成 DSE 方法对比矩阵。
- 自动分类论文方法。
- 检索“用了 BO 的论文”或“用了 GNN 的论文”。
- 生成 Survey 报告。
- 做 benchmark、metric、baseline 统计。

### 16.4 具体实现流程

新增流程是：

```text
扫描 PDF
-> 读取前几页文本
-> 构造结构化抽取 prompt
-> 调用 DeepSeek LLM
-> 解析 JSON
-> 字段归一化
-> 写入 SQLite
```

默认只读取前 4 页，因为标题、作者、摘要、方法总览通常都在前几页。

如果某些论文的方法细节在后面，可以后续扩展成：

```text
先抽 front matter
再用 RAG 检索 method / experiment / limitation 相关 chunk
最后合并抽取
```

### 16.5 命令行用法

通过独立模块运行：

```powershell
python -m rag_pipeline.paper_info --limit 3
```

全量抽取：

```powershell
python -m rag_pipeline.paper_info --all
```

只打印结果，不写 SQLite：

```powershell
python -m rag_pipeline.paper_info --limit 1 --dry-run
```

查看最近抽取记录：

```powershell
python -m rag_pipeline.paper_info --list --limit 10
```

也可以通过 `main.py` 入口运行：

```powershell
python main.py --extract-paper-info --paper-info-limit 3
python main.py --paper-info-list
```

### 16.6 SQLite 表结构

抽取结果保存在：

```text
rag_pipeline/data/paper_info.sqlite3
```

核心表：

```text
paper_info
```

主要列包括：

```text
relative_path
file_name
category
title
authors_json
year
venue
abstract
keywords_json
method_name
problem
core_idea
optimization_algorithm
model_or_agent
design_variables_json
objectives_json
benchmarks_json
baselines_json
metrics_json
main_results_json
limitations_json
tags_json
raw_json
updated_at
```

### 16.7 面试版总结

可以这样回答：

> 我在基础 RAG 入库之外增加了论文结构化信息抽取。系统会读取 PDF 前几页文本，调用 LLM 按固定 JSON schema 抽取 metadata 和 method information。metadata 包括标题、作者、年份、会议、摘要、关键词；method information 包括方法名、解决问题、核心思想、优化算法、模型或代理、设计变量、优化目标、benchmark、baseline、metrics、主要结果和局限性。抽取结果保存到本地 SQLite，后续可以用于方法分类、DSE 方法对比矩阵和长任务 Survey 报告生成。

## 17. DSE 方法对比矩阵 MCP Tool

### 17.1 功能定位

`generate_dse_method_matrix` 是一个 MCP tool，用于从已经抽取好的 `paper_info.sqlite3` 中生成 DSE/MOO 方法对比矩阵。

它不直接读 PDF，也不重新调用 LLM，而是复用第 16 节的结构化论文信息。

链路是：

```text
PDF
-> paper_info.py 抽取 metadata / method information
-> paper_info.sqlite3
-> method_matrix.py 生成对比矩阵
-> MCP tool: generate_dse_method_matrix
-> Agent 调用并基于矩阵回答或总结
```

### 17.2 对比矩阵包含什么

默认矩阵字段包括：

```text
Method
Paper
Year
Problem
Core Idea
Optimizer
Model/Agent
Objectives
Benchmarks
Baselines
Metrics
Main Results
Limitations
Source
```

这些字段来自 `paper_info` 表中的结构化方法信息。

### 17.3 MCP Tool 参数

工具名：

```text
generate_dse_method_matrix
```

主要参数：

```text
keyword: 关键词过滤
category: 类别或路径过滤，例如 DSE
method_names: 指定方法名列表
year_from: 起始年份
year_to: 截止年份
limit: 最大行数
output_format: markdown / json / csv
save_path: 可选，保存到项目目录下的 reports/
```

### 17.4 实现模块

核心生成逻辑：

```text
rag_pipeline/method_matrix.py
```

MCP 注册：

```text
mcp_server/tools/method_matrix.py
mcp_server/server.py
```

### 17.5 面试版总结

可以这样回答：

> 我把 DSE 方法对比矩阵做成了 MCP tool，而不是只做成本地脚本。底层 `method_matrix.py` 从 `paper_info.sqlite3` 读取结构化论文信息，按关键词、类别、年份或方法名过滤，然后生成 Markdown、JSON 或 CSV 对比矩阵。MCP tool `generate_dse_method_matrix` 把这个能力暴露给 Agent，因此用户问“比较这些 DSE 方法”时，Agent 可以先调用工具生成结构化矩阵，再基于矩阵做总结。这让项目从普通 RAG 问答进一步扩展到结构化论文分析。
